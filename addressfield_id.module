<?php

/**
 * @file
 * addressfield_id primary module file.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function addressfield_id_ctools_plugin_directory($module, $plugin) {
  if ($module == 'addressfield') {
    return 'plugins/' . $plugin;
  }
}
